# SolidFreeCAD 3.1.5 generated Kotlin source fix

This synchronization commit triggers the Android universal build after registering the generated V27 workbench in AndroidSourceSet.kotlin for AGP 9 built-in Kotlin. CI and post-build inspection must confirm that SolidFreeCadWorkbenchActivityV27 and ProgressiveWorkbenchLoader are actual DEX class definitions. Temporary build branch only; do not merge into main.
