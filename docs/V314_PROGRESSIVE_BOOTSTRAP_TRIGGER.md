# SolidFreeCAD 3.1.4 progressive bootstrap

This synchronization commit triggers the Android universal build after moving the reflection loader into the generated workbench source, preserving lifecycle handling, Android 16 startup guards and ARM32/ARM64 packaging. Temporary build branch only; do not merge into main.
